import { css } from '@emotion/react'

import * as utils from '../../styles/utils'

const sidebar = css`
  background: ${utils.sidebarBgColor};
  color: ${utils.sidebarColor};
  width: ${utils.sidebarWidth}px;
  padding: 25px;
`
export default sidebar
